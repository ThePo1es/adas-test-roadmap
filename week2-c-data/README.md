# Week 2 — C 기초 + 주행 데이터 분석

**주간 목표:** 제어기(ECU) 코드를 "읽을 수 있는" 수준의 C + 실제 주행 데이터로 EDA 리포트 1편.
**왜 C인가:** 공고의 C/C++ 요구 + 제어기 검증 업무는 C 코드 기반. 이번 주는 읽기·기초 쓰기까지만.
**교재:** 모두의 코드 "씹어먹는 C"(무료) 또는 학교 C 교재

---

## Day 1 (월) — C 환경 + 기본 문법

| 시간 | 할 일 |
|------|------|
| 0:00~1:00 | 컴파일러 준비: Windows면 MinGW(gcc) 설치 or 온라인 컴파일러(godbolt, onlinegdb). `hello.c` 컴파일→실행 |
| 1:00~1:30 | 컴파일 4단계(전처리→컴파일→어셈블→링크) 개념만. "파이썬은 인터프리터, C는 컴파일"의 의미 |
| 1:30~3:00 | 자료형(int/float/double/char), printf 포맷(%d %f %s), scanf. **파이썬과의 차이를 표로 정리**: 정적타입, 세미콜론, 중괄호, main 함수 |
| 3:00~4:30 | 코드업에서 파이썬으로 이미 푼 입출력 문제 5개를 C로 다시 풀기 (코드업은 C 제출 지원) |
| 4:30~5:00 | notes-day1.md + 커밋 |

**완료 기준:** C로 두 수를 입력받아 사칙연산 결과를 출력하는 코드를 안 보고 쓴다.

## Day 2 (화) — 포인터 / 배열 / 함수 (이번 주 고비)

| 시간 | 할 일 |
|------|------|
| 0:00~2:00 | 포인터: 주소(&), 역참조(*), 포인터 변수 선언. **종이에 메모리 그림을 그려가며** 학습. int a=5; int *p=&a; 에서 p, *p, &a 가 각각 뭔지 |
| 2:00~3:00 | 배열과 포인터의 관계, 배열을 함수 인자로 넘기면 왜 크기를 같이 넘겨야 하는지 |
| 3:00~4:30 | 실습 3개: ① swap 함수(포인터로) ② 배열 평균 함수 `float mean(float *arr, int n)` ③ 배열 최댓값의 "인덱스"를 반환하는 함수 |
| 4:30~5:00 | 포인터를 그림 1장으로 설명한 노트 작성 + 커밋 |

**완료 기준:** swap을 포인터 없이 만들면 왜 안 되는지 설명할 수 있다.

## Day 3 (수) — 구조체 + 비트연산 → 과제 1

| 시간 | 할 일 |
|------|------|
| 0:00~1:00 | struct, typedef. 예: `typedef struct { float speed; int rpm; } SensorData;` |
| 1:00~2:00 | 비트연산자 & \| ^ << >>, 비트마스크로 특정 비트 추출: `(x >> 4) & 0x0F` 손으로 계산 연습 5개 |
| 2:00~4:30 | **과제 1: `assignments/can_frame.c` 구현** — 8바이트 프레임에서 차속/RPM/기어 추출 (파일 내 스펙 참고). 기대 출력과 일치할 때까지 |
| 4:30~5:00 | 커밋. little-endian이 뭔지 2줄 메모 |

**완료 기준:** `speed = 100.00 km/h, rpm = 1000.0, gear = D` 가 정확히 출력된다.

## Day 4 (목) — 실제 주행 데이터 EDA (Python 복귀)

| 시간 | 할 일 |
|------|------|
| 0:00~1:00 | Kaggle 가입 → "OBD-II dataset" 또는 "vehicle telemetry" 검색 → 시계열 주행 데이터셋 1개 다운로드 (없으면 임의의 차량 센서 CSV 아무거나) |
| 1:00~2:30 | 데이터 파악: `df.info()`, `df.describe()`, 결측치 개수, 샘플링 주기 확인(시간 열 diff) |
| 2:30~4:30 | 시각화 4종: ① 속도 시계열 ② 속도 히스토그램 ③ RPM vs 속도 산점도 ④ 가속도 시계열(diff로 계산). 전부 축라벨+제목 |
| 4:30~5:00 | 그래프 png와 함께 커밋 |

**완료 기준:** "이 데이터는 총 몇 분 주행, 최고속도 얼마, 결측치 어디"를 말할 수 있다.

## Day 5 (금) — 신호처리 기초

| 시간 | 할 일 |
|------|------|
| 0:00~1:00 | 개념: 샘플링 주파수(Hz), 왜 실차 데이터에 노이즈가 끼는지(센서, 진동, 양자화) |
| 1:00~2:00 | 1차 저역통과필터(LPF) 수식 이해: `y[k] = a*y[k-1] + (1-a)*x[k]`, a와 컷오프의 관계는 정성적으로만 |
| 2:00~4:00 | 실습: Day 4 데이터의 가속도 신호에 ① 이동평균(Week1 함수 재사용) ② LPF(a=0.9, 0.95) 적용, 원본과 3종 비교 플롯 |
| 4:00~4:30 | 칼만필터는 "노이즈 모델 기반 최적 추정"이라는 용도만 메모 (구현은 나중) |
| 4:30~5:00 | 커밋 |

**완료 기준:** "이동평균 vs LPF 차이"를 그래프를 근거로 2줄 설명할 수 있다.

## Day 6 (토) — 미니 시험 리포트 → 과제 2

| 시간 | 할 일 |
|------|------|
| 0:00~0:30 | 리포트 뼈대 잡기: ① 데이터 개요 ② 분석 방법 ③ 결과(그래프) ④ 결론/한계 |
| 0:30~4:00 | **과제 2: `assignments/driving_report.md` 작성** — Day 4~5 분석을 마크다운 리포트로. 그래프 이미지 삽입, 표 사용. **"시험 직무의 절반은 문서"** |
| 4:00~4:30 | 소리 내어 읽고 어색한 문장 수정 (남에게 보여줄 문서 기준) |
| 4:30~5:00 | 커밋 |

**완료 기준:** 처음 보는 사람이 리포트만 읽고 데이터가 뭔지, 뭘 알아냈는지 이해할 수 있다.

## Day 7 (일) — 복습/정리

| 시간 | 할 일 |
|------|------|
| 0:00~1:30 | 포인터 재복습: Day 2 실습 3개를 안 보고 다시 구현 |
| 1:30~3:00 | can_frame.c에 신호 1개(예: 브레이크 압력, byte5~6) 스스로 추가 정의하고 파싱 |
| 3:00~5:00 | `TIL-week2.md` + 주간 커밋 |
