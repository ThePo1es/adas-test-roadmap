/* 과제 2: 단독 AEB 로직 (CAN 없이, 아두이노 단독)
 *
 * HC-SR04 거리 측정 -> 임계값 이하이면 모터 정지 + 부저 + 빨간 LED.
 * 배선: Trig->D9, Echo->D8, 모터드라이버 IN->D5/D6, 부저->D7, LED->D4, 임계조절 가변저항->A0
 *
 * 요구:
 * 1. pulseIn으로 거리(cm) 측정 함수 readDistance() 구현
 * 2. A0 가변저항으로 임계거리(10~50cm) 실시간 매핑
 * 3. 거리<임계 -> 모터 정지+부저 ON+LED ON, 아니면 주행
 * 실패 주의: pulseIn 타임아웃 미설정 시 센서 미인식하면 loop가 멈춘다 -> timeout 인자 줄 것
 */
// TODO: 직접 구현
