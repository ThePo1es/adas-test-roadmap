# Week 1 — Python 기초 + Git (탈LLM 주간)

**주간 목표:** LLM 없이 200줄짜리 파이썬 스크립트를 스스로 짤 수 있다. Git으로 매일 커밋하는 습관이 몸에 붙는다.
**교재:** 점프 투 파이썬(위키독스 무료) / 백준 온라인 저지 "단계별로 풀어보기"

---

## Day 1 (월) — 환경 구축 + Git

| 시간 | 할 일 |
|------|------|
| 0:00~1:00 | VSCode, Python 3.12, Git 설치. VSCode에 Python 확장 설치. 터미널에서 `python --version`, `git --version` 확인 |
| 1:00~1:30 | 터미널 명령어 직접 타이핑 연습: `cd`, `dir`(ls), `mkdir`, `del`(rm), `type`(cat), 상대경로 vs 절대경로 |
| 1:30~3:00 | Git 개념 학습: 저장소, 스테이징, 커밋, 원격(remote). `git init` → `add` → `commit` → GitHub 저장소 생성 → `push` 전체 흐름을 **3번 반복** |
| 3:00~4:30 | 실습: 이 로드맵 저장소를 본인 GitHub에 push. 텍스트 파일 수정→커밋 10회, `git log`/`git diff` 읽기, `.gitignore`에 `__pycache__/` 추가 |
| 4:30~5:00 | 오늘 배운 명령어를 `week1-python-git/notes-day1.md`에 본인 말로 정리 + 커밋 |

**완료 기준:** 본인 GitHub에 저장소가 올라가 있고, 커밋 10개 이상, git add/commit/push를 안 보고 친다.

## Day 2 (화) — 변수 / 자료형 / 조건문 / 반복문

| 시간 | 할 일 |
|------|------|
| 0:00~2:00 | 점프 투 파이썬 2장(자료형: 숫자/문자열/리스트/튜플/딕셔너리), 3장(if/while/for). 예제를 **눈으로 읽지 말고 전부 직접 타이핑** |
| 2:00~4:30 | 백준 단계별 "입출력과 사칙연산" 전체(약 10문제) + "조건문" 전체(약 7문제). **규칙: 30분 막히면 풀이 보고, 다음날 아침 다시 스스로 풀기** |
| 4:30~5:00 | 틀린 문제 번호와 이유를 notes-day2.md에 기록 + 커밋 |

**완료 기준:** 백준 15문제 이상 AC(맞았습니다). f-string으로 소수점 2자리 출력을 안 보고 쓴다.

## Day 3 (수) — 반복문 심화 / 함수 / 자료구조

| 시간 | 할 일 |
|------|------|
| 0:00~1:30 | 점프 투 파이썬 4장(함수: 매개변수, 반환값, 기본값 인자). 리스트 컴프리헨션 문법 |
| 1:30~4:30 | 백준 단계별 "반복문" 전체 + "1차원 배열" 전체 + "함수" 단계. 목표 12문제 |
| 4:30~5:00 | "함수를 왜 쓰는가"를 notes-day3.md에 3줄로 본인 말로 설명 + 커밋 |

**완료 기준:** 리스트에서 최댓값/평균을 내장함수 없이 for문으로 구하는 코드를 3분 안에 쓴다.

## Day 4 (목) — 파일 I/O / 예외처리 → 과제 1

| 시간 | 할 일 |
|------|------|
| 0:00~1:00 | 점프 투 파이썬 파일 읽기/쓰기(`open`, `readlines`, `with`), 문자열 `split`/`strip` |
| 1:00~1:30 | try-except 기본, `float()` 변환 실패 처리 |
| 1:30~2:00 | 실습 데이터 만들기: 엑셀이나 메모장으로 `sample.csv` 작성 (time,speed,accel 3열×50행, 중간에 빈 줄 2개와 문자 오타 1개 일부러 넣기) |
| 2:00~4:30 | **과제 1: `assignments/csv_parser.py` 구현** (pandas 금지). 열별 min/max/평균 + 건너뛴 행 수 출력 |
| 4:30~5:00 | 커밋 + 막힌 지점 기록 |

**완료 기준:** 오염된 행이 있어도 죽지 않고 통계를 출력한다.

## Day 5 (금) — NumPy + Matplotlib → 과제 2

| 시간 | 할 일 |
|------|------|
| 0:00~1:00 | NumPy 공식 "absolute beginners" 튜토리얼: 배열 생성(`arange`, `linspace`), 사칙연산, 슬라이싱 |
| 1:00~2:00 | Matplotlib: `plot`, `xlabel/ylabel`, `legend`, `grid`, `savefig`. 그래프에 축 라벨 없으면 시험 리포트에서 빵점이라는 것 기억 |
| 2:00~4:30 | **과제 2: `assignments/moving_average.py` 구현** — 노이즈 sin 신호 + 이동평균(직접 구현) N=5/20/50 비교 플롯 |
| 4:30~5:00 | N이 커질 때 생기는 지연(lag) 원인을 주석 3줄로 작성 + 그래프 png 포함 커밋 |

**완료 기준:** 그래프 1장에 4개 곡선 + 범례 + 축라벨이 다 있다.

## Day 6 (토) — Pandas 입문

| 시간 | 할 일 |
|------|------|
| 0:00~1:30 | Pandas "10 minutes to pandas": `read_csv`, `head`, `describe`, 불리언 필터, 새 열 추가 |
| 1:30~2:30 | Day 4의 sample.csv를 pandas로 다시 읽기 — 내가 만든 파서와 결과 비교 (검증 습관) |
| 2:30~4:30 | 실습: 가속도 열 계산 `df['accel_calc'] = df['speed'].diff() / df['time'].diff()`, 급가속(accel > 3 m/s²) 구간 필터링, 해당 구간 시계열 플롯에 색칠 표시 |
| 4:30~5:00 | 커밋 |

**완료 기준:** "급가속 구간이 몇 개, 각각 몇 초 구간인지" 출력하는 스크립트 완성.

## Day 7 (일) — 복습/정리

| 시간 | 할 일 |
|------|------|
| 0:00~2:00 | 이번 주 풀이 본 백준 문제를 **안 보고** 재도전 |
| 2:00~3:30 | 과제 1, 2 코드 리팩터링: 함수 분리, 변수명 정리 |
| 3:30~5:00 | `TIL-week1.md` 작성: 배운 것 / 막힌 것 / 다음 주 보완점 + 주간 마무리 커밋 |
